import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  pressed: {
    opacity: 0.5,
  },
  itemContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginVertical: 8,
  },
  nameContainer: {
    padding: 16,
  },
});

export default styles;
